import { MNEEBalance, SendMNEE } from "./mnee.types.js";
interface Mnee {
    balance(address: string): Promise<MNEEBalance>;
    transfer(request: SendMNEE[], wif: string): Promise<{
        txid?: string;
        rawtx?: string;
        error?: string;
    }>;
}
declare const mnee: (apiToken?: string) => Mnee;
export default mnee;
